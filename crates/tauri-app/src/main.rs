//! nextool-gui:Tauri 桌面 GUI 入口
//!
//! 占位:后续步骤集成 Tauri command 层,调用 nextool-core 暴露给前端。

fn main() {
    // Tauri GUI 尚未集成;CLI 已可用。见 docs/v0.1/tech.md。
    eprintln!("nextool-gui: Tauri GUI 尚未集成,请使用 nextool-cli。");
    std::process::exit(0);
}
