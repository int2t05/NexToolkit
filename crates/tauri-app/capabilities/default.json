{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "NexToolkit 默认权限:核心命令 + 文件选择对话框,最小权限原则",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default"
  ]
}
