{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "NexToolkit 默认权限:仅允许调用自有核心命令,最小权限原则",
  "windows": ["main"],
  "permissions": [
    "core:default"
  ]
}
