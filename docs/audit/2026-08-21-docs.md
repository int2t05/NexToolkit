# NexToolkit 文档与代码同步审计报告

**审计日期:** 2026-08-21
**审计范围:** `docs/`(PRD.md、v0.1/PRD.md、v0.1/tech.md、research/、audit/)+ README.md + CHANGELOG.md + CONTRIBUTING.md ↔ 代码(`crates/{core,cli,tauri-app}/`、`src/`、`Cargo.toml`、`package.json`、`tauri.conf.json`、`.github/workflows/`)
**审计方式:** 子 agent 只读核对,逐条比对文档声称与代码实际,证据标位置(文件:行)。只读代码,不改动代码与文档。

---

## 审计范围与方法

| 文档 | 代码对照源 |
|---|---|
| `docs/PRD.md` | 产品级,无逐工具矩阵;核对用户故事/边界/验收 |
| `docs/v0.1/PRD.md` | 工具矩阵(33)、用户故事 CLI 示例、测试策略、分发 |
| `docs/v0.1/tech.md` | 技术栈、workspace 结构、Tool 契约、命令、避坑、UNVERIFIED |
| `docs/research/2026-08-21-local-oss-toolbox.md` | 调研报告(单份),核对溯源残留 |
| `docs/audit/*.md` | 既有两份审计报告,核对内部死引用 |
| `README.md` / `CHANGELOG.md` / `CONTRIBUTING.md` | 工具矩阵、CLI 示例、构建命令、版本、状态 |

代码事实基线(核对锚点):

- core 50 个 `pub fn`(`grep pub fn crates/core/src`),按 7 模块分布;**无 `http` 相关函数**,无 `reqwest` 依赖(`crates/core/Cargo.toml` 无 reqwest)。
- CLI 7 一级子命令(`crates/cli/src/main.rs:34-49`),二级子命令为嵌套 `nextool <group> <tool> [mode/args]`。
- tauri command 47 个(`crates/tauri-app/src/commands.rs` + `lib.rs:21-69` `generate_handler!`),与 `src/tools.ts` 47 个 `id` 一一相等(GUI 审计已确认)。
- `src/tools.ts` nettime 组 5 条(ipcalc/timestamp_to_human/timestamp_from_human/cron_next/dns_lookup),**无 http**;`hmac_compute` 标 `group: 'generate'`(`tools.ts:83`)。
- 版本:`Cargo.toml` workspace 0.2.0 / `package.json` 0.2.0 / `tauri.conf.json` 0.2.0,一致。

---

## 逐项发现

### 1. 工具矩阵一致性

**✗ 不一致 — `http` 工具残留 + `hmac` 分组冲突 + 计数错误**

| 位置 | 声称 | 实际 | 判定 |
|---|---|---|---|
| `docs/v0.1/PRD.md:19` | Net/Time 含 `http`(5 工具,总计 33) | core 无 http 函数、CLI 无 http 子命令、tauri 无 http command、tools.ts 无 http | ✗ 残留 |
| `README.md:26` | Net/Time = `ipcalc · timestamp · cron · dns`(4 工具,无 http) | 与代码一致 | ✓ |
| `README.md:16` | 标题"共 33 个" | 实际实现 32 个(http 未实现) | ✗ 计数错 |
| `docs/v0.1/PRD.md:18` / `README.md:25` | Crypto 组含 `hmac` | 代码将 hmac 归 Generate(`main.rs:41` doc、"hash/hmac/..."、`tools.ts:83` group='generate'、`commands.rs` "// 生成器" 段、`generate_cmd.rs:22` `Hmac`) | ✗ 分组冲突 |
| `docs/PRD.md`(产品级) | 无逐工具矩阵,无 http | — | ✓ |
| `docs/v0.1/tech.md` §4 | 仅列七组名,无逐工具 | — | ✓(但 §1 TLS 行为 http 而存,见项 10) |

**逻辑工具实际计数(按代码分组):** encode 5 + convert 5 + format 4 + generate 6(含 hmac)+ text 5 + crypto 3(aes-gcm/rsa/kdf)+ nettime 4 = **32**。
**PRD 计数(含 http、hmac 归 crypto):** 33。差异来源:http 未实现(+1)、hmac 归属不同(不影响总数)。

**修复建议:**
- `docs/v0.1/PRD.md:19` 删 `http`,Net/Time 改为 `ipcalc、timestamp、cron、dns`,标题"共 33 个"改"共 32 个"。
- `docs/v0.1/PRD.md:18` 与 `README.md:25`:hmac 从 Crypto 移到 Generators(Crypto 改 `aes-gcm、rsa、kdf`;Generators 加 `hmac`)。或反向:代码改 hmac 归 crypto——但代码 4 处一致归 generate,改文档成本更低且代码已稳定。
- `README.md:16` "共 33 个"改"共 32 个"。

---

### 2. 命令示例一致性

**✗ `docs/v0.1/PRD.md` 用户故事 CLI 示例系统性错误(扁平结构);README 示例正确**

实际 CLI 为嵌套:`nextool <group> <tool> [mode/args]`(依据:`main.rs:34-49` Command 枚举 + 各 `*_cmd.rs` 二级 Subcommand)。

| PRD 示例(`docs/v0.1/PRD.md`) | 行 | 实际命令 | 判定 |
|---|---|---|---|
| `nextool base64 encode "Hello"` | 25 | `nextool encode base64 encode "Hello"` | ✗ 缺 group |
| `nextool base64 decode` | 26 | `nextool encode base64 decode` | ✗ |
| `nextool json-yaml to-yaml` | 31 | `nextool convert json-yaml to`(mode 为 `to`/`from`,非 `to-yaml`) | ✗ |
| `nextool regex match` | 36 | `nextool text regex-match <pattern>` | ✗ |
| `nextool regex replace` | 37 | `nextool text regex-replace <pattern> <replacement>` | ✗ |
| `nextool diff` | 41 | `nextool text diff` | ✗ |
| `nextool aes-gcm encrypt` | 45 | `nextool crypto aes-encrypt --password <pw>` | ✗ |
| `nextool ipcalc` | 50 | `nextool nettime ipcalc` | ✗ |
| `nextool timestamp to-human --tz Asia/Shanghai` | 54 | `nextool nettime ts-to-human <ts> --tz Asia/Shanghai`(缺必填 ts 参数) | ✗ |

README 示例(`README.md:31-35`)全部正确:
- `nextool encode base64 encode "Hello"` ✓
- `nextool generate hash sha256 abc` ✓(对应 `generate_cmd.rs:17` `Hash { algo, input }`)
- `nextool crypto aes-encrypt --password pw "Secret"` ✓(对应 `crypto_cmd.rs:16` `AesEncrypt`)

`docs/PRD.md:13` `nextool <工具> <参数>` 为抽象描述,可读作完整路径,边界模糊(见修复清单 P2)。

**修复建议:** `docs/v0.1/PRD.md` 用户故事 9 处示例全部改为嵌套形式,对齐 README 与实际 clap 结构。

---

### 3. 技术栈声明

**✗ tech.md §6 包管理器 pnpm 残留(应 npm);其余技术栈一致**

| 维度 | tech.md 声称 | 代码实际 | 判定 |
|---|---|---|---|
| Tauri 版本 | 2.11.x(`tech.md:10`) | `package.json` `@tauri-apps/cli ^2.11.4`、`api ^2.11.1`;`tauri-app/Cargo.toml` `tauri = "2"` | ✓ |
| Svelte 5 | `tech.md:11` | `package.json` `svelte ^5.0.0`;`svelte.config.js` `runes: true` | ✓ |
| MSVC target | `tech.md:16` | `release.yml:28` `x86_64-pc-windows-msvc`;`CONTRIBUTING.md:5` "MSVC target" | ✓ |
| 包管理器 | tech.md §6 用 `pnpm`(行 93/95/98/99/103) | CHANGELOG:20 "前端包管理用 npm";README/CONTRIBUTING/CI 均用 `npm`/`npm ci`;`tauri.conf.json:7-8` `npm run dev`/`npm run build` | ✗ 残留 |
| TLS rustls | `tech.md:14` reqwest+rustls-tls | core 无 reqwest 依赖(`crates/core/Cargo.toml`),http 工具未实现 | ✗ 决策行为已不存在的功能(见项 10) |

**修复建议:** tech.md §6 五处 `pnpm` → `npm`(`pnpm install`→`npm install`、`pnpm tauri dev`→`npm run tauri dev`、`pnpm tauri build`→`npm run tauri build`、`pnpm tauri build --no-bundle`→`npm run tauri build -- --no-bundle`、`pnpm check`→`npm run check`)。

---

### 4. 构建命令一致性

**✗ tech.md §6 pnpm 命令过时;README/CONTRIBUTING 基本一致(轻微差异)**

| 命令来源 | 命令 | 实际可用 | 判定 |
|---|---|---|---|
| `README.md:52` | `cargo build -p nextool-cli --release` | `release.yml:43` 同;`cli/Cargo.toml` bin `nextool` | ✓ |
| `README.md:53` | `npm install && npm run tauri build` | `package.json` `tauri` script;`tauri.conf.json` beforeBuildCommand `npm run build` | ✓ |
| `README.md:54` | `cargo test --workspace` | workspace 可跑 | ✓ |
| `CONTRIBUTING.md:16` | `cargo clippy --workspace --all-targets -- -D warnings` | CI 用 `cargo clippy -p nextool-core -p nextool-cli --all-targets`(`ci.yml:30`,因 gui 需前端 dist) | ⚠ 本地跑 `--workspace` 含 tauri-app 需先 `npm run build` 产 dist,否则 generate_context! 失败 |
| `CONTRIBUTING.md:14` | `cargo fmt --check --all` | CI `cargo fmt --check --all`(`ci.yml:28`) | ✓ |
| `CONTRIBUTING.md:17` | `npm run check` | `package.json` `check` script | ✓ |
| `tech.md:93-103` | `pnpm install`/`pnpm tauri dev`/`pnpm tauri build`/`pnpm check`/`cargo fmt --check`(缺 `--all`) | 应 npm;CI/README 用 `--all` | ✗ |
| `tech.md:97` | `cargo build -p nextool-cli --release` | 同 README | ✓ |

**修复建议:** tech.md §6 改 npm + `cargo fmt --check --all`;CONTRIBUTING `clippy` 命令加注"需先 `npm run build` 产前端 dist"或改为 `-p nextool-core -p nextool-cli` 与 CI 对齐。

---

### 5. 版本号一致性

**✗ README 状态标签 v0.1 过时;CHANGELOG 重复 [0.1.0] 标题;工具计数错**

| 来源 | 版本 | 判定 |
|---|---|---|
| `Cargo.toml:7` workspace.package | 0.2.0 | ✓ |
| `package.json:4` | 0.2.0 | ✓ |
| `tauri.conf.json:5` | 0.2.0 | ✓ |
| `CHANGELOG.md:7` | `[0.2.0] — 2026-08-21` | ✓ |
| `CHANGELOG.md:27` 与 `:29` | `## [0.1.0] — 2026-08-21` 出现两次(连续重复标题) | ✗ 重复 |
| `README.md:69` | "v0.1:CLI + GUI 双入口可用" | ✗ GUI 为 0.2.0 新增(CHANGELOG:9),状态应标 v0.2.0 |
| `README.md:16` | "工具矩阵(v0.1,共 33 个)" | ✗ 计数 32(见项 1) |

**修复建议:**
- `CHANGELOG.md` 删除第 27 或 29 行的重复 `## [0.1.0]` 标题。
- `README.md:69` "v0.1:" → "v0.2.0:"。
- `README.md:16` "共 33 个" → "共 32 个"。

---

### 6. 死链接 / 死引用

**✗ 多处死路径与死设计引用**

| 位置 | 引用 | 实际 | 判定 |
|---|---|---|---|
| `docs/v0.1/tech.md:42` | 结构图 `├── test/` | 根目录无 `test/` 目录;CLI 集成测试在 `crates/cli/tests/cli_smoke.rs` | ✗ 死路径 |
| `docs/v0.1/tech.md:31` | mermaid `TEST[test/<br/>集成测试]` | 同上 | ✗ |
| `docs/v0.1/tech.md:143` | "CLI 集成测试(`test/cli/`)" | 实际 `crates/cli/tests/` | ✗ |
| `docs/v0.1/PRD.md:83` | "CLI:`test/cli/` 用 assert_cmd" | 实际 `crates/cli/tests/cli_smoke.rs` 用 assert_cmd(`cli/Cargo.toml:19`) | ✗ |
| `docs/v0.1/tech.md:56-66` §3 | `pub trait Tool { fn name/run }` 契约 | core 无 trait(`grep trait Tool crates/core/src` 零命中),实现为按模块自由函数 | ✗ 死设计引用 |
| `docs/v0.1/tech.md:110-133` §7 | `impl Tool for Base64` 代码示例 | 同上,trait 不存在 | ✗ |
| `docs/v0.1/PRD.md:18` | "RSA 密钥生成/加解密/签名" | core 有 rsa_keygen/rsa_encrypt/rsa_decrypt,**无 rsa_sign**;"签名"未实现 | ✗ 超声称 |
| `crates/tauri-app/src/commands.rs` | RSA 仅暴露 rsa_keygen | rsa_encrypt/rsa_decrypt 在 core+CLI 有,但 tauri command 与 tools.ts 未暴露 | ✗ GUI 缺口 |
| `docs/audit/2026-08-21-gui.md:51` | "详见 `docs/research/`(it-tools/Tauri+Svelte 参考分析)" | `docs/research/` 仅 1 份报告(local-oss-toolbox),无 GUI 参考分析报告;参考在 `reference/competitors/`(gitignored)+ memory | ✗ 死引用 |
| README 内 `](docs/...md)` 链接 | 6 条 | 全部存在(PRD/v0.1/PRD/v0.1/tech/research/audit/CHANGELOG) | ✓ |
| `docs/v0.1/PRD.md:3`、`tech.md:3` 互链 | 引用 `docs/PRD.md`/`docs/v0.1/tech.md` | 存在 | ✓ |
| tech.md `reference/competitors/...` | 7 个竞品路径 | `reference/competitors/` 本地存在(Boop/CyberChef/DevToys/OpenCovibe/comine/it-tools/tauri2-svelte5-shadcn);gitignored,文档已注明"本地分析" | ⚠ 仓库读者不可核实,但已声明 |

**修复建议:**
- tech.md §2 结构图删 `test/` 节点或改注"集成测试在各 crate 的 `tests/` 下";§8 与 v0.1/PRD.md:83 的 `test/cli/` → `crates/cli/tests/`。
- tech.md §3/§7:删 `Tool` trait 契约与示例,改为描述实际"按域模块自由函数 + 统一 `ToolError`"设计(lib.rs 已是此结构)。
- v0.1/PRD.md:18:RSA 描述删"签名"(或标注远期);GUI 侧补 rsa_encrypt/rsa_decrypt 暴露(或 PRD 标注 GUI 仅暴露 keygen)。
- gui audit:51:改指向 `reference/competitors/`(本地)或补一份 docs/research/ GUI 参考报告。

---

### 7. 死指令(文档"统一/必须"正文未做到)

**✗ 两处死指令 + 一处未实现 UI**

| 位置 | 指令 | 实际 | 判定 |
|---|---|---|---|
| `docs/v0.1/tech.md:154` §9 避坑 #4 | "commands.rs 按域拆:避免单巨型文件编译爆炸" | `commands.rs` 单文件 274 行含全部 47 command(仅注释分段,未按域拆文件) | ✗ 死指令 |
| `docs/v0.1/tech.md:56-66` §3 | Tool trait 为"工具统一契约" | 无 trait,自由函数 | ✗ 死指令(同项 6) |
| `docs/v0.1/PRD.md:68` | "常用转换快捷卡片(base64、hash、json-fmt、jwt 等)" | `App.svelte` 无快捷卡片,仅侧栏列表 + 搜索框 | ✗ 未实现 |
| `docs/v0.1/PRD.md:73` 规则 1 | "core 纯逻辑,不依赖 Tauri/clap/stdin/stdout" | core 零 IO 依赖(`lib.rs` 纯逻辑) | ✓ |
| `docs/v0.1/PRD.md:75` 规则 2 | "CLI 薄:clap 解析后直接调 core" | 各 `*_cmd.rs` 仅转发 | ✓ |
| `docs/v0.1/PRD.md:76` 规则 3 | "GUI 薄:#[tauri::command] 调 core,前端只渲染" | `commands.rs` 仅转发;`App.svelte` 仅渲染 | ✓ |
| `docs/v0.1/PRD.md:74` 规则 4 | "禁止 mock,真实数据" | `cli/tests/cli_smoke.rs` 真起二进制;core 单测真实输入 | ✓ |

**修复建议:**
- tech.md §9 #4:要么按指令拆 `commands.rs` 为 `commands/{encode,convert,...}.rs`,要么改指令为"单文件注释分段,规模可控时不再拆"——后者与实际相符。
- v0.1/PRD.md:68:删快捷卡片指令,或改为"搜索框 + 侧栏分组"描述实际交互。

---

### 8. 产物路径 ↔ CI 清单

**✗ 便携 GUI 原始 exe 未在 CI 产出;安装版/CLI 二进制一致**

| 文档声称产物 | CI `release.yml` 实际产物 | 判定 |
|---|---|---|
| CLI 单二进制三平台(`v0.1/PRD.md:89`) | `cli-release` 产 `nextool-cli-{linux/windows/macos}`(行 22-51) | ✓ |
| Windows 安装版 msi/nsis(`v0.1/PRD.md:91`) | `gui-release` tauri-action + `bundle.targets: "all"` → msi/nsis | ✓ |
| Linux deb/AppImage(`v0.1/PRD.md:91`) | 同上 → deb/AppImage | ✓ |
| macOS dmg(`v0.1/PRD.md:91`) | 同上 → dmg | ✓ |
| **便携 GUI Windows exe(skip WebView2)**(`v0.1/PRD.md:90`) | release.yml 未跑 `--no-bundle`,tauri-action "all" 仅产安装器,不产便携原始 exe | ✗ 缺口 |
| `tauri.conf.json:38-40` `webviewInstallMode: {type: "skip"}` | skip 生效(安装器不嵌/不下载 WebView2) | ✓ 配置在 |
| `tech.md:99` `pnpm tauri build --no-bundle`(便携 exe 命令) | release.yml 无此步 | ✗ 命令存在但 CI 未执行 |

**修复建议:** 二选一:(a)release.yml gui-release 增加一步 `npm run tauri build -- --no-bundle` 上传便携 exe;或 (b)v0.1/PRD.md 分发段删"便携 GUI exe"声称,仅保留安装版(与 CI 实际产物对齐)。

---

### 9. 溯源残留

**✓ 文档无标签式溯源残留;tech.md 含合法引用与 UNVERIFIED 标注(非残留);research 单份报告无第二份**

| 文档 | 检查 | 判定 |
|---|---|---|
| `docs/PRD.md` | 无"（vX.X 新增）"标签、无版本号正文、无变更指针 | ✓ |
| `docs/v0.1/PRD.md` | 同上 | ✓ |
| `docs/v0.1/tech.md` | `tauri-v2.11.5(2026-07-01)` 为依赖版本引用(合法证据);`UNVERIFIED:` 为文档自声明标注(非溯源标签);无项目内版本溯源 | ✓ |
| `docs/research/2026-08-21-local-oss-toolbox.md` | 竞品版本号(v11.0.0 等)为调研证据;§9.3 UNVERIFIED 为调研局限标注;无项目溯源 | ✓ |
| `docs/research/` 报告数 | 仅 1 份(local-oss-toolbox);任务提"两份报告"未兑现——GUI 设计参考分析存 `reference/competitors/`(gitignored)+ memory,未落 docs/research/ | ⚠ 缺口(非溯源残留) |
| `tech.md:16` §1 Windows 链接器行 | gnu→MSVC 切换叙事为历史决策过程;CLAUDE.md"remove transitional logic"——决策已定(MSVC),gnu 叙事为过渡残留 | ⚠ 轻微(P2) |

**修复建议:** tech.md §1 Windows 链接器行可压缩为"采用 MSVC target(Tauri Windows 官方推荐);gnu target 因 windows-sys 0.59 FFI 栈溢出 ICE 弃用",删调试过程叙事。docs/research/ 缺 GUI 参考报告:补一份或 gui audit 改指向 `reference/competitors/`。

---

### 10. UNVERIFIED 项

**✗ 三处 UNVERIFIED 已可验证但仍标 UNVERIFIED(或已失效)**

| 位置 | UNVERIFIED 内容 | 实际状态 | 判定 |
|---|---|---|---|
| `tech.md:14` | TLS rustls "未在本机实测跨编译" | http 工具未实现、reqwest 未引入(`core/Cargo.toml` 无 reqwest),该决策为不存在功能的预留 | ✗ 失效(应删或标"引擎层/http 工具引入时再验") |
| `tech.md:87` | `webviewInstallMode` 精确枚举值"未在 context7 命中" | `tauri.conf.json:38-40` 已确认 `{ "type": "skip" }` | ✗ 已验证,应更新 |
| `tech.md:174` | "Tauri gnu-target GUI 链接兼容性未实测" | gnu target 已弃用切 MSVC;MSVC GUI 已 CI 三平台编译验证(`CHANGELOG:23` tauri-build 三平台绿、`gui audit:39` GUI 三平台编译成功) | ✗ 已失效/已验证,应更新 |
| `tech.md:174` | "DevToys 接口确切路径未逐一核实" | 仍为调研未核实项 | ✓ 保留 |
| `tech.md:174` | "各竞品实际产物体积未编译测量" | 仍为未实测项 | ✓ 保留 |

**修复建议:**
- tech.md:14 TLS 行:删除或改为"http 工具归引擎层后续,引入时再定 TLS 实现"。
- tech.md:87:删 UNVERIFIED,改"`tauri.conf.json` 已确认 `webviewInstallMode: { type: "skip" }`"。
- tech.md:174:删 gnu-target UNVERIFIED,改"MSVC target GUI 已 CI 三平台编译验证(见 CHANGELOG 0.2.0 验证段)"。

---

## 修复清单(按优先级)

### P0 — 误导性 drift(用户/开发者照此操作会出错)

1. **`docs/v0.1/PRD.md:19` 删 `http`** + 标题"共 33 个"→"共 32 个"(项 1)
2. **`README.md:16` "共 33 个"→"共 32 个"**(项 1/5)
3. **`docs/v0.1/PRD.md` 用户故事 9 处 CLI 示例改嵌套形式**(项 2,对齐 README 与实际 clap)
4. **`CHANGELOG.md:27/29` 删除重复 `## [0.1.0]` 标题**(项 5)

### P1 — 明确 drift(文档与代码事实矛盾)

5. **`docs/v0.1/tech.md:93-103` 五处 `pnpm`→`npm`**(项 3/4)
6. **`docs/v0.1/tech.md:56-66` §3 + `:110-133` §7 删 `Tool` trait 契约与示例**,改述实际"按域模块自由函数 + 统一 ToolError"(项 6/7)
7. **`docs/v0.1/tech.md:31/42/143` + `docs/v0.1/PRD.md:83` `test/cli/`→`crates/cli/tests/`,结构图删 `test/` 节点**(项 6)
8. **`docs/v0.1/tech.md:87` webviewInstallMode UNVERIFIED 改已验证**(项 10)
9. **`docs/v0.1/tech.md:174` gnu-target UNVERIFIED 改 MSVC CI 已验证**(项 10)
10. **`docs/v0.1/tech.md:14` TLS rustls 行删除或标 http 归后续**(项 3/10)
11. **`docs/v0.1/PRD.md:18` + `README.md:25` hmac 从 Crypto 移到 Generators**(项 1)
12. **`README.md:69` "v0.1:"→"v0.2.0:"**(项 5)
13. **`docs/v0.1/PRD.md:18` RSA 删"签名"超声称**;GUI 补 rsa_encrypt/rsa_decrypt 暴露或 PRD 标注 GUI 仅 keygen(项 6)
14. **CI `release.yml` 增便携 exe 产物** 或 `v0.1/PRD.md:90` 删便携 exe 声称(项 8)

### P2 — 轻微 drift(不影响操作,影响一致性)

15. **`docs/v0.1/tech.md:154` §9 #4 commands.rs 按域拆**:改指令为"单文件注释分段"对齐实际,或拆文件(项 7)
16. **`docs/v0.1/PRD.md:68` 快捷卡片指令**:删或改为"搜索 + 侧栏分组"(项 7)
17. **`docs/audit/2026-08-21-gui.md:51` 死引用**:指向 `reference/competitors/` 或补 docs/research/ GUI 报告(项 6)
18. **`docs/v0.1/tech.md:16` gnu→MSVC 叙事压缩**(项 9)
19. **`CONTRIBUTING.md:16` clippy 命令**加注需前端 dist 或改为 `-p` 形式对齐 CI(项 4)
20. **`docs/v0.1/tech.md:102` `cargo fmt --check` 补 `--all`**(项 4)
21. **`docs/PRD.md:13` `nextool <工具> <参数>`** 改为 `nextool <分组> <工具> <参数>` 明示嵌套(项 2)

---

## 总体结论

**结论:** 文档与代码存在系统性 drift,集中在 `docs/v0.1/PRD.md` 用户故事示例与 `docs/v0.1/tech.md` 技术细节两处。

- **P0 4 项**(http 残留 + 计数错 + PRD CLI 示例全错 + CHANGELOG 重复标题):误导性强,照文档操作直接失败,须立即修复。
- **P1 10 项**(pnpm 残留、Tool trait 死引用、test/ 死路径、3 处 UNVERIFIED 过时、hmac 分组、版本标签、RSA 超声称、便携 exe 缺口):文档与代码事实矛盾,影响可信度。
- **P2 7 项**(死指令、快捷卡片未实现、audit 死引用、叙事冗余、命令轻微差异):一致性瑕疵。

**一致项(已核实无 drift):**
- 7 分组名跨 tools.ts/commands.rs/main.rs/PRD/README 一致。
- 47 个 tauri invoke 命令名三处集合相等(tools.ts id == commands.rs pub fn == lib.rs generate_handler)。
- 技术栈 Tauri 2.11 / Svelte 5 / MSVC / 跨平台矩阵一致(tech.md pnpm 为唯一例外)。
- 版本 0.2.0 跨 Cargo/package/tauri.conf 一致。
- CLI 嵌套结构与 README 示例一致。
- CSP 锁紧 + capabilities 最小权限与 tech.md 避坑 #5 一致。
- CLI 单二进制 + 安装版产物与 release.yml 一致(便携 exe 为唯一缺口)。
- core 纯逻辑、CLI/GUI 薄封装规则在代码中落实。
- 调研报告与 PRD 无标签式溯源残留。

**建议执行顺序:** 先 P0(4 项,用户可见误导),再 P1 tech.md 块(pnpm + Tool trait + test 路径 + UNVERIFIED,可一次改完),最后 P1 PRD/README 块与 P2。修复后重跑本文档逐项核对验证。
