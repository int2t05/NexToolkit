# GUI 集成纯净审计报告

> 审计日期:2026-08-21 ｜ 范围:Tauri GUI(Rust `crates/tauri-app/` + 前端 `src/`)
> 方式:子 agent 独立只读审计 + 集成者复核,对照 A-F 残留检查表

## 审计范围

Rust GUI(lib.rs/commands.rs/main.rs + Cargo.toml/tauri.conf.json/capabilities/build.rs)+ 前端(App.svelte/main.ts/bindings.ts/tools.ts/app.css + 构建 config)。

## 结论

**修复后可提交。** 阻断项 1 + 清理项 4 全部修复。

## 发现与修复

| # | 级别 | 位置 | 问题 | 修复 |
|---|---|---|---|---|
| 1 | **阻断** | commands.rs password_generate | `any` 守卫非对称:用户选"仅符号"时 `any=true` 强制 upper/lower/digits 开,覆盖用户意图;且 core 已校验全 false | 删 `any` 及 `any ||` 覆盖,直传 `parse_bool` 结果,全 false 交 core 报错 |
| 2 | 清理 | App.svelte:2 | `type ToolParam` 死 import(类型由 `Tool.params` 推断,名未用) | 删除 |
| 3 | 清理 | svelte.config.js | 注释把 tsconfig 的排除行为归到本文件(死指令) | 改写为"经 tsconfig include 限定到 src/" |
| 4 | 清理 | lib.rs/bindings.ts/vite.config.ts | 标签式溯源指针 + 路径缺 `reference/competitors/` 前缀(A1/C9) | 删溯源尾注 |
| 5 | 轻微 | lib.rs:5 | 注释正文 "Tauri 2" 版本号 | 随 #4 删除该行 |

## 无问题项

- **A3 分组**:tools.ts/commands.rs 按功能域非批次。
- **B4/B6 装饰冗余**:`// 编解码` 单行区隔(非横幅);47 command 样板是 Tauri 必需。
- **C12 产物路径**:frontendDist `../../dist` 与 vite outDir `dist` 一致。
- **C13 共享结构**:ToolError→CmdError 转换正确;7 分组名 tools.ts 与 commands.rs 一致。
- **E15/E16 语言平台**:注释全中文;跨平台配置(windows_subsystem/mobile_entry_point/crate-type/skip)意图明确。
- **TODO 残留**:零命中。
- **invoke 命令名一致性**:47 个 tools.ts id == commands.rs pub fn == lib.rs generate_handler,三处集合完全相等。
- **CSP/安全**:`script-src 'self'` 严格;`style-src 'unsafe-inline'` 为 Svelte 作用域样式必需;capabilities 仅 `core:default` + windows:main 最小权限。
- **frontendDist 路径**:`../../dist` 正确。
- **横幅一致性**:commands.rs 无 `// ====` 装饰横幅,仅单行区隔。

## 验证证据

- CI 全绿:frontend ✓ + core-cli(clippy+test)✓ + tauri-build(macOS/Ubuntu/Windows)✓。
- 前端:vite build 通过(51.5KB JS gzip 19.6KB),svelte-check 0 error 0 warning。
- GUI 三平台编译成功(Tauri 2.11 + Svelte 5)。

## 设计依据

前端/桌面端设计爬取参考(reference/competitors/,git 忽略):
- it-tools(Vue3 工具集):布局/工具元数据/复制交互/i18n/主题模式
- tauri2-svelte5-shadcn:Tauri2+Svelte5 runes+invoke 封装/错误处理/capabilities
- OpenCovibe:集中式 API/Builder 链/command 分组
- comine:ts-rs 类型绑定/capabilities scope/流式 IPC(远期)

详见 `docs/research/`(it-tools/Tauri+Svelte 参考分析)与 memory `reference-competitors`。
